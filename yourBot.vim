#!/usr/bin/env bash
# Bot implementation in vim
# vim: set syntax=off :
exec vim -n -u DEFAULTS --cmd 'set loadplugins' -c 'normal gg}jy$@"' "${0}"

j0"ay$j0"by$@a@b
:silent edit http://localhost:5000/simulator/state:0file:file state
:s/^\v(.*)\[(.*)\],(.*)$/\2\r\r\3gg:s/},{/}\r{/ggg:.,/^$/-1 v/"ownerId":null/dgg:.,/^$/-1 sortgg0fxlllyt,:.+1,/^$/-1 normal fx"gg0/"y"/llllyt}:.+1,/^$/-1 normal /"y"/"
